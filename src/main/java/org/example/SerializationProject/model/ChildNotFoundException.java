package org.example.SerializationProject.model;

// Exception for if they pass a last name that doesn't exist

public class ChildNotFoundException extends Exception {
    @Override
    public String getMessage() {

        return null;
    }
}
